{
  "name": "jk-ristinolla-server",
  "version": "0.0.1",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build:ui": "rm -rf build && cd front && npm run build && cp -r build .."
  },
  "author": "JKajalin",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
